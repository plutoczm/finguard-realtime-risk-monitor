services:
  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: finguard-kafka
    ports:
      - "127.0.0.1:9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:29092,CONTROLLER://0.0.0.0:29093,EXTERNAL://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:29092,EXTERNAL://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
    volumes:
      - ./scripts:/scripts:ro
    healthcheck:
      test: ["CMD", "kafka-topics", "--bootstrap-server", "localhost:9092", "--list"]
      interval: 10s
      timeout: 5s
      retries: 12

  flink-jobmanager:
    image: flink:1.19.1-scala_2.12-java17
    container_name: finguard-flink-jobmanager
    command: jobmanager
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "127.0.0.1:8081:8081"
    environment:
      JOB_MANAGER_RPC_ADDRESS: flink-jobmanager
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        parallelism.default: 2
        taskmanager.numberOfTaskSlots: 4
        execution.checkpointing.interval: 60s
    volumes:
      - ./flink-job/target:/opt/flink/usrlib
      - ./data:/opt/finguard/data

  flink-taskmanager:
    image: flink:1.19.1-scala_2.12-java17
    container_name: finguard-flink-taskmanager
    command: taskmanager
    depends_on:
      - flink-jobmanager
    environment:
      JOB_MANAGER_RPC_ADDRESS: flink-jobmanager
      TASK_MANAGER_NUMBER_OF_TASK_SLOTS: 4
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        parallelism.default: 2
        taskmanager.numberOfTaskSlots: 4
    volumes:
      - ./flink-job/target:/opt/flink/usrlib
      - ./data:/opt/finguard/data

  ai-copilot:
    build:
      context: .
      dockerfile: ai_service/Dockerfile
    container_name: finguard-ai-copilot
    profiles: ["ai"]
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      FINGUARD_AI_MODEL: ${FINGUARD_AI_MODEL:-gpt-5-mini}
      FINGUARD_AI_DB_PATH: /app/state/finguard_ai.sqlite3
      FINGUARD_AI_FAILURE_THRESHOLD: ${FINGUARD_AI_FAILURE_THRESHOLD:-3}
      FINGUARD_AI_COOLDOWN_SECONDS: ${FINGUARD_AI_COOLDOWN_SECONDS:-30}
      FINGUARD_AI_MAX_CONCURRENCY: ${FINGUARD_AI_MAX_CONCURRENCY:-4}
      FINGUARD_AI_BULKHEAD_WAIT_MS: ${FINGUARD_AI_BULKHEAD_WAIT_MS:-25}
      FINGUARD_CORS_ORIGINS: ${FINGUARD_CORS_ORIGINS:-http://127.0.0.1:8090,http://localhost:8090}
    ports:
      - "127.0.0.1:8091:8091"
    volumes:
      - finguard_ai_state:/app/state
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8091/readyz')"]
      interval: 15s
      timeout: 5s
      retries: 5

volumes:
  finguard_ai_state:
